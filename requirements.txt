# Discord library (pinned to a specific upstream commit for stable behavior with this codebase)
git+https://github.com/DisnakeDev/disnake.git@96ed4459eff7f7b8a841f62e5d542d44dc721895

# Core utils
python-dotenv>=1.0.0
user_agent>=0.1.10
requests>=2.32.0
humanize>=4.9.0
aiofiles>=23.2.1
psutil>=5.9.8
packaging>=23.2
emoji>=2.10.0
regex>=2024.5.10
cachetools>=5.3.0
rapidfuzz>=3.6.0
ruamel.yaml>=0.18.0
nest_asyncio>=1.6.0

# Database
motor>=3.4.0
tinymongo>=0.2.0
# Imported directly by utils/db.py. tinymongo lists it transitively, but that
# did not resolve on a fresh Pterodactyl install and the bot crashed on boot
# with ModuleNotFoundError — declare what we import.
tinydb-serialization>=2.1.0
# Imported directly by utils/music/errors.py; otherwise only present via motor.
pymongo>=4.9
aiosqlite>=0.20.0
dnspython>=2.6.1

# Lyrics
syncedlyrics>=0.7.0
pylrc>=0.1.2

# YT/Audio extraction
yt-dlp[default]>=2025.08.11

# Web server
tornado>=6.4.2

# HTTP / networking
# aiohttp is imported directly in 14 modules; it arrives with disnake today,
# but a dependency we import belongs here in its own right.
aiohttp>=3.9.0,<4.0
urllib3>=2.5.0
async-timeout>=4.0.3
curl_cffi>=0.7.0

# Browser-based scrapers (used by some audio sources)
nodriver==0.32
undetected-chromedriver==3.5.5

# REPL helpers (optional; only the disnake-compatible build works — official PyPI jishaku is discord.py only).
# Install manually if needed, e.g.: pip install "jishaku @ git+https://github.com/<your-fork>/jishaku.git"

# Python 3.12+: setuptools is no longer bundled with venv
setuptools>=70.0.0; python_version>='3.12'

# Python 3.13+: audioop was removed from the stdlib
audioop-lts; python_version>='3.13'

# Faster event loop on POSIX (not supported on Windows)
uvloop>=0.19.0; sys_platform != 'win32'
